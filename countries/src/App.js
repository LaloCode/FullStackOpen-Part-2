import { useState } from 'react';

const App = ({ countries }) => {
  const [filter, setFilter] = useState('')
  const [showCountries, setCountries] = useState([])

  return (
    <div>Hi</div>
  );
}

export default App;
